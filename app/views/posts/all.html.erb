<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Posts</title>
</head>
<body>
  <header>
    <%= render 'partial/header' %>
  </header>

    <% if user_signed_in? %>
      <%= render 'partial/current_user_area' %>
    <% end %>

  <main>
  <div class="w-[90%] max-w-[1200px] mx-auto mt-[50px] text-center">
    <div class="w-full py-[20px] text-center">
      <h1 class="font-extrabold text-[32px] mb-[20px]">All Posts</h1>
          <div class="grid gap-6 grid-cols-1 md:grid-cols-2 lg:grid-cols-4 mt-6">
          <% if @posts.any? %>
          <% @posts.each do |post| %>
          <%= link_to post_path(post) do %>
        <div class="w-full max-w-sm mx-auto rounded-md shadow-md overflow-hidden">
            <div class="flex items-end justify-end h-56 w-full">
            <% if post.image.attached? %>
                <%= image_tag post.image, class: 'object-cover w-full h-full' %>
                <% else %>
                <%= image_tag 'rpa-logo.png', class: 'object-cover w-full h-full' %>
            <% end %>
            </div>
            <div class="px-5 py-3 text-left">
                <h3 class="text-gray-700 uppercase"><%= post.title %></h3>
                <span class="text-gray-500 mt-2"><%= post.user.name %></span>
            </div>
        </div>
        <% end %>
        <% end %>
        <% end %>
    </div>
    </div>
  </div>
      </div>
    </div>
  </div>
</main>
</body>
</html>
